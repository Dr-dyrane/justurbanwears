import assert from "node:assert/strict";
import { existsSync, readFileSync } from "node:fs";
import test from "node:test";
const root=process.cwd();const shell=readFileSync(`${root}/components/studio/app-shell.tsx`,"utf8");const home=readFileSync(`${root}/components/studio/studio-home.tsx`,"utf8");const models=readFileSync(`${root}/components/studio/model-atelier.tsx`,"utf8");const wardrobe=readFileSync(`${root}/components/studio/wardrobe-workbench.tsx`,"utf8");const dossier=readFileSync(`${root}/components/studio/garment-dossier.tsx`,"utf8");const directCaptures=readFileSync(`${root}/components/studio/draft-direct-captures.tsx`,"utf8");const operations=readFileSync(`${root}/components/studio/operations-desk.tsx`,"utf8");const css=readFileSync(`${root}/app/foundation.css`,"utf8");const atelierCss=readFileSync(`${root}/app/studio-atelier.css`,"utf8");const mobileCss=readFileSync(`${root}/app/mobile-experience.css`,"utf8");const appleBars=readFileSync(`${root}/app/apple-mobile-bars.css`,"utf8");const rootLayout=readFileSync(`${root}/app/layout.tsx`,"utf8");
test("Studio mobile separates five visible destinations from one contextual action",()=>{for(const label of["Home","Wardrobe","Orders","Ops","More"])assert.match(shell,new RegExp(label));assert.match(shell,/aria-label="Studio tabs"/);assert.match(shell,/className="shop-mobile-context shop-dock-lens studio-mobile-context"/);assert.match(shell,/useRegisteredStudioMobileAction/);assert.match(shell,/registeredAction\?\.invokeTargetId/);assert.match(wardrobe,/invokeTargetId: "piece-primary-action"/);assert.match(appleBars,/grid-template-columns:repeat\(5,minmax\(0,1fr\)\)/);assert.match(appleBars,/min-height:44px/);assert.match(appleBars,/font-size:11px/);assert.match(rootLayout,/apple-mobile-bars\.css\?raw/);assert.doesNotMatch(shell,/studio-mobile-fab/)});
test("task-first records, garments, and inventory use approved media",()=>{assert.match(home,/studioGarmentCover/);assert.match(home,/studio-attention-primary/);assert.match(home,/studio-recent-row/);assert.match(wardrobe,/studioGarmentCover/);assert.match(operations,/studioGarmentCover/);assert.match(atelierCss,/\.studio-attention-layout/);assert.match(atelierCss,/\.studio-recent-row/);assert.match(atelierCss,/\.studio-garment-grid/);assert.match(atelierCss,/\.studio-inventory-row-trigger/);assert.match(mobileCss,/studio-atelier-home > \.studio-atelier-hero[\s\S]*?display: none/)});
test("compact garment rows expose preview and one Piece workspace",()=>{assert.match(wardrobe,/label=\{`Preview \$\{garment\.title\}`\}/);assert.match(wardrobe,/aria-label=\{`Open \$\{garment\.title\}`\}/);assert.match(wardrobe,/className="studio-garment-disclosure"/);assert.match(wardrobe,/garmentDossierHref/);assert.doesNotMatch(wardrobe,/Manage draft|Prepare listing|Open listing/);assert.match(appleBars,/font-size:11px/)});
test("garments open one Piece workspace with one truthful next action",()=>{assert.match(wardrobe,/className="studio-draft-sheet studio-piece-sheet"/);assert.match(wardrobe,/<PieceWorkspaceView/);assert.match(wardrobe,/nextAction\.label/);assert.match(dossier,/<PieceWorkspaceView/);assert.doesNotMatch(dossier,/useStudioMobileAction|selectPieceWorkspace/);assert.match(wardrobe,/useStudioMobileAction\(mobileAction\)/);assert.match(dossier,/candidate\.privateWardrobeItemId === requestedId/)});
test("model segmented content can render without the portrait obstruction",()=>{assert.match(models,/activeView === "profile" \? <div className=/);assert.match(models,/is-panel-only/);assert.match(models,/pending=\{viewPending\}/);assert.match(atelierCss,/\.studio-model-list[\s\S]*?scroll-snap-type: x proximity/)});
test("operator copy and recovery stay action-led",()=>{assert.match(home,/scenario[\s\S]*?Simulator · not saved/);assert.match(home,/Workspace saved/);assert.match(directCaptures,/Magic Wand/);assert.match(directCaptures,/Create AI preview/);assert.match(directCaptures,/Yes, it matches/);assert.match(wardrobe,/Shop preview/);assert.match(wardrobe,/Live in Shop/);assert.match(operations,/Manage stock and returns\./);assert.match(css,/padding: 30px 16px calc\(104px \+ env\(safe-area-inset-bottom, 0px\)\)/);assert.match(atelierCss,/@media \(prefers-reduced-motion: reduce\)/);assert.match(mobileCss,/mask-image: linear-gradient/)});
test("documented browser evidence is complete",()=>{for(const image of["01-home.png","02-records.png","03-model-readiness.png","04-wardrobe.png","05-inventory.png","06-garment-intake.png"])assert.equal(existsSync(`${root}/docs/screenshots/studio-ux/${image}`),true,image)});
