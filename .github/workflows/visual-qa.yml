name: Visual QA

on:
  push:
    branches:
      - agent/visual-qa
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: visual-qa-${{ github.ref }}
  cancel-in-progress: true

jobs:
  visual-audit:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      CI: true
      NEXT_TELEMETRY_DISABLED: "1"
      STUDIO_AI_ENGINE_AUTH_MODE: ""
      DATABASE_URL: ""
      DATABASE_URL_UNPOOLED: ""
      PUBLIC_BLOB_READ_WRITE_TOKEN: ""
      PRIVATE_BLOB_READ_WRITE_TOKEN: ""
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22.19.0
          cache: npm

      - name: Install application dependencies
        run: npm ci

      - name: Install visual-audit browser
        run: |
          npm install --no-save --no-package-lock playwright@1.54.2 axe-core@4.10.3
          npx playwright install --with-deps chromium

      - name: Start the application in Studio seed mode
        run: |
          mkdir -p visual-qa
          npm run dev -- --host 127.0.0.1 --port 3000 > /tmp/justurbanwears-dev.log 2>&1 &
          APP_PID=$!
          echo "$APP_PID" > /tmp/justurbanwears-dev.pid
          for attempt in $(seq 1 45); do
            if ! kill -0 "$APP_PID" 2>/dev/null; then
              cat /tmp/justurbanwears-dev.log
              exit 1
            fi
            if curl --fail --silent --max-time 3 http://127.0.0.1:3000/favicon.svg >/dev/null; then
              exit 0
            fi
            sleep 2
          done
          cat /tmp/justurbanwears-dev.log
          exit 1

      - name: Capture and audit every experience surface
        run: node scripts/visual-qa.mjs

      - name: Preserve application log
        if: always()
        run: |
          mkdir -p visual-qa
          cp /tmp/justurbanwears-dev.log visual-qa/dev-server.log || true

      - name: Upload visual evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: justurbanwears-visual-qa-${{ github.run_id }}
          path: visual-qa
          if-no-files-found: error
          retention-days: 7
